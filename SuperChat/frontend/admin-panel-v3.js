<style id="admin-panel-v3-style">
#screen-container.admin-panel-v3-scroll{position:relative!important;overflow-y:auto!important;overflow-x:hidden!important;height:auto!important;min-height:100dvh!important;-webkit-overflow-scrolling:touch;overscroll-behavior:auto;padding-bottom:20px}
html.admin-panel-v3-open,body.admin-panel-v3-open{overflow:auto!important;height:auto!important}
#admin_screen_content.admin-panel-v3-root{position:relative!important;display:block!important;width:100%!important;min-height:100dvh!important;height:auto!important;overflow:visible!important;transform:none!important;animation:none!important;padding:0!important}
#admin_panel_v3{display:block;width:100%;max-width:100%;min-height:100dvh;padding:12px;box-sizing:border-box;overflow:visible}
#admin_panel_v3 *{box-sizing:border-box}
#admin_panel_v3 .ap3-header,#admin_panel_v3 .ap3-stat-grid,#admin_panel_v3 .ap3-tabs,#admin_panel_v3 .ap3-panel{position:static!important;overflow:visible!important}
#admin_panel_v3 .ap3-header{display:flex;gap:12px;align-items:flex-start;flex-wrap:wrap;padding:16px;border-radius:24px;background:linear-gradient(180deg,rgba(17,24,39,.96),rgba(15,23,42,.92));border:1px solid rgba(255,255,255,.08);box-shadow:0 18px 40px rgba(0,0,0,.22);margin-bottom:12px}
#admin_panel_v3 .ap3-title{min-width:0;flex:1}
#admin_panel_v3 .ap3-title h2{margin:0;font-size:28px;line-height:1.1;font-weight:800}
#admin_panel_v3 .ap3-title p{margin:6px 0 0;color:#94a3b8;font-size:14px}
#admin_panel_v3 .ap3-stat-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;margin-bottom:12px}
#admin_panel_v3 .ap3-stat{padding:16px;border-radius:20px;background:rgba(15,23,42,.92);border:1px solid rgba(255,255,255,.06);display:flex;gap:12px;align-items:center}
#admin_panel_v3 .ap3-stat .value{font-size:26px;font-weight:800;color:#fff}
#admin_panel_v3 .ap3-stat .label{font-size:12px;color:#94a3b8;margin-top:2px}
#admin_panel_v3 .ap3-tabs{display:flex;gap:8px;flex-wrap:wrap;margin-bottom:12px}
#admin_panel_v3 .ap3-tab{display:inline-flex;align-items:center;gap:8px;padding:12px 14px;border-radius:16px;border:1px solid rgba(255,255,255,.08);background:rgba(15,23,42,.84);color:#e2e8f0;font-weight:700}
#admin_panel_v3 .ap3-tab.is-active{background:linear-gradient(180deg,rgba(14,165,233,.24),rgba(59,130,246,.18));border-color:rgba(56,189,248,.45)}
#admin_panel_v3 .ap3-search{display:flex;gap:10px;align-items:center;padding:12px 14px;border-radius:18px;background:rgba(15,23,42,.92);border:1px solid rgba(255,255,255,.06);margin-bottom:12px}
#admin_panel_v3 .ap3-search input{width:100%;min-width:0;background:transparent;border:none;outline:none;color:#fff}
#admin_panel_v3 .ap3-list{display:flex;flex-direction:column;gap:10px}
#admin_panel_v3 .ap3-card{width:100%;padding:14px;border-radius:20px;background:rgba(15,23,42,.92);border:1px solid rgba(255,255,255,.06);color:#fff}
#admin_panel_v3 button.ap3-card{text-align:left;cursor:pointer}
#admin_panel_v3 .ap3-row{display:flex;gap:12px;align-items:flex-start;min-width:0}
#admin_panel_v3 .ap3-avatar{width:52px;height:52px;border-radius:50%;object-fit:cover;flex:0 0 auto}
#admin_panel_v3 .ap3-meta{min-width:0;flex:1}
#admin_panel_v3 .ap3-name{display:flex;gap:8px;align-items:center;flex-wrap:wrap;font-weight:800}
#admin_panel_v3 .ap3-sub{font-size:13px;color:#94a3b8;margin-top:2px;word-break:break-word}
#admin_panel_v3 .ap3-pres{font-size:12px;margin-top:4px}
#admin_panel_v3 .ap3-chips{display:flex;gap:6px;flex-wrap:wrap;margin-top:8px}
#admin_panel_v3 .ap3-side{display:flex;flex-direction:column;gap:8px;align-items:flex-end;flex:0 0 auto}
#admin_panel_v3 .ap3-actions{display:flex;gap:8px;flex-wrap:wrap;margin-top:12px}
#admin_panel_v3 .ap3-two{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}
#admin_panel_v3 .ap3-empty{padding:28px 16px;border-radius:20px;background:rgba(15,23,42,.7);border:1px dashed rgba(148,163,184,.28);color:#94a3b8;text-align:center}
#admin_panel_v3 .ap3-actor{display:flex;gap:10px;align-items:center;padding:10px;border-radius:16px;background:rgba(255,255,255,.04);cursor:pointer}
#admin_panel_v3 .ap3-actor img{width:44px;height:44px;border-radius:50%;object-fit:cover}
#admin_panel_v3 .ap3-evidence{display:flex;gap:10px;align-items:flex-start;margin-top:12px;padding:10px;border-radius:16px;background:rgba(255,255,255,.04)}
#admin_panel_v3 .ap3-evidence img{width:90px;height:90px;object-fit:cover;border-radius:14px}
#admin_panel_v3 .ap3-message{margin-top:12px;padding:12px;border-radius:16px;background:rgba(255,255,255,.04)}
#admin_panel_v3 .ap3-quote{padding:12px;border-radius:16px;background:rgba(255,255,255,.04);color:#e2e8f0}
@media (max-width:640px){#admin_panel_v3{padding:10px}#admin_panel_v3 .ap3-header{padding:14px;border-radius:20px}#admin_panel_v3 .ap3-title h2{font-size:24px}#admin_panel_v3 .ap3-stat-grid,#admin_panel_v3 .ap3-two{grid-template-columns:minmax(0,1fr)}#admin_panel_v3 .ap3-tabs{flex-wrap:nowrap;overflow-x:auto;overflow-y:hidden;scrollbar-width:none}#admin_panel_v3 .ap3-tabs::-webkit-scrollbar{display:none}#admin_panel_v3 .ap3-tab{white-space:nowrap;flex:0 0 auto}}
</style>
<script id="admin-panel-v3-script">
(function(){
  function esc(v){ return window.escapeHtml ? window.escapeHtml(v ?? '') : String(v ?? '').replace(/[&<>\"']/g, m => ({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m])); }
  function jsq(v){ return String(v ?? '').replace(/\\/g,'\\\\').replace(/'/g,"\\'").replace(/\n/g,' '); }
  function rolesNorm(list){ return Array.isArray(list) ? list.map(r => typeof r === 'string' ? {key:r,name:r} : (r||{})) : []; }
  function myRoleKeys(){ return Array.isArray(window.currentUser?.roles) ? window.currentUser.roles.map(r => typeof r === 'object' ? String(r.key||'') : String(r||'')).filter(Boolean) : []; }
  function roleMap(){ return {project_team:100,special_admin:95,owner1:92,owner2:91,main_admin:85,deputy_admin:80,senior_admin:70,admin:60,senior_moderator:50,moderator:40,junior_moderator:30,helper:10}; }
  function maxRole(keys,isMod){ let m=isMod?45:0; const map=roleMap(); (keys||[]).forEach(k=>{ if(map[k]) m=Math.max(m,map[k]); }); return m; }
  function canManageTarget(target){ const mine=maxRole(myRoleKeys(), !!window.currentUser?.is_moderator); const theirs=maxRole(rolesNorm(target?.roles).map(r=>String(r.key||'')), !!target?.is_moderator); return String(window.currentUser?.id||'') !== String(target?.id||'') && mine > theirs; }
  function badge(icon,text,extra=''){ return `<span class="admin-v2-chip ${extra}">${window.adminSvg?window.adminSvg(icon):''}<span>${esc(text)}</span></span>`; }
  function stat(icon,value,label){ return `<div class="ap3-stat"><div>${window.adminSvg?window.adminSvg(icon):''}</div><div><div class="value">${esc(value)}</div><div class="label">${esc(label)}</div></div></div>`; }
  function presence(u){ const p=u&&u.presence?u.presence:null; const base=p?(p.status_label||(u.online?'в сети':'был(а) недавно')):(u.online?'в сети':'был(а) недавно'); const extra=p&&p.status_text?` • ${p.status_text}`:''; return `${base}${extra}`; }
  function adminRoot(){ return document.getElementById('admin-panel-v3-content'); }
  function ensureScroll(enabled){ const sc=document.getElementById('screen-container'); document.documentElement.classList.toggle('admin-panel-v3-open', !!enabled); document.body.classList.toggle('admin-panel-v3-open', !!enabled); if(sc) sc.classList.toggle('admin-panel-v3-scroll', !!enabled); }
  const TABS=[{key:'users',title:'Пользователи',icon:'users'},{key:'banned',title:'Блокировки',icon:'block'},{key:'reports',title:'Жалобы',icon:'bell'},{key:'channels',title:'Каналы',icon:'chat'},{key:'roles',title:'Роли',icon:'shield'}];
  function userCard(u){ const roles=rolesNorm(u.roles); return `<button type="button" class="ap3-card" onclick="selectAdminUser('${jsq(u.id)}')"><div class="ap3-row"><img src="${esc(u.avatar||'/default.png')}" class="ap3-avatar" alt=""><div class="ap3-meta"><div class="ap3-name"><span>${esc(u.name||u.username||'Пользователь')}</span>${u.is_verified&&(window.verifiedBadgeSvg)?window.verifiedBadgeSvg():''}${u.online?'<span class="w-2 h-2 rounded-full bg-green-500"></span>':''}</div><div class="ap3-sub">@${esc(u.username||'user')}</div><div class="ap3-pres ${u.online?'text-green-400':'text-slate-400'}">${esc(presence(u))}</div><div class="ap3-chips">${roles.map(r=>`<span class="role-tag role-fx text-xs" style="background:${esc(r.color_bg||'#334155')};color:${esc(r.color_text||'#fff')}">${esc(r.name||r.key||'')}</span>`).join('')}${u.is_moderator?badge('shield','Модератор'):''}${(u.muted_until&&(u.muted_until*1000>Date.now()))?badge('bell','Мут'):''}</div></div><div class="ap3-side">${badge('bolt', String(u.stars||0))}</div></div></button>`; }
  function renderUsers(){ const root=adminRoot(); if(!root) return; const users=window.adminPanelData?.users||[]; root.innerHTML=`<div class="ap3-search">${window.adminSvg?window.adminSvg('search'):''}<input id="ap3-user-search" type="text" placeholder="Поиск по имени или логину"></div><div id="ap3-users-list" class="ap3-list">${users.length?users.map(userCard).join(''):'<div class="ap3-empty">Пользователей нет</div>'}</div>`; const input=document.getElementById('ap3-user-search'); if(input){ input.addEventListener('input',()=>{ const q=input.value.toLowerCase().trim(); const items=!q?users:users.filter(u=>String(u.name||'').toLowerCase().includes(q)||String(u.username||'').toLowerCase().includes(q)); const list=document.getElementById('ap3-users-list'); if(list) list.innerHTML=items.length?items.map(userCard).join(''):'<div class="ap3-empty">Ничего не найдено</div>'; }); } }
  function renderBanned(){ const root=adminRoot(); if(!root) return; const list=window.adminPanelData?.banned_users||[]; const muted=(window.adminPanelData?.users||[]).filter(u=>u.muted_until&&(u.muted_until*1000>Date.now())).length; root.innerHTML=`<div class="ap3-stat-grid">${stat('block',list.length,'Активных блокировок')}${stat('bell',muted,'Активных мутов')}</div><div class="ap3-list">${list.length?list.map(u=>`<div class="ap3-card"><div class="ap3-row"><img src="${esc(u.avatar||'/default.png')}" class="ap3-avatar" alt=""><div class="ap3-meta"><div class="ap3-name">${esc(u.name||'Пользователь')}</div><div class="ap3-sub">@${esc(u.username||'user')}</div><div class="text-sm text-red-300 mt-2">Причина: ${esc(u.block_reason||'Нарушение правил')}</div><div class="ap3-chips mt-2">${u.ban_ip?badge('block','IP'):''}${u.ban_device?badge('shield','Устройство'):''}${badge('note',u.expires_at_formatted||'Навсегда')}</div></div><div class="ap3-side"><button type="button" class="primary-button text-xs px-3 py-2 bg-green-600 hover:bg-green-700" onclick="adminAction('${jsq(u.id)}','unban')">${window.adminSvg?window.adminSvg('unlock'):''}<span>Разбанить</span></button></div></div></div>`).join(''):'<div class="ap3-empty">Нет активных блокировок</div>'}</div>`; }
  function actorCard(actor,label){ return `<button type="button" class="ap3-actor" onclick="${actor&&actor.id?`selectAdminUser('${jsq(actor.id)}')`:'void(0)'}"><img src="${esc((actor&&actor.avatar)||'/default.png')}" alt=""><div class="min-w-0"><div class="text-xs text-slate-400">${label}</div><div class="font-semibold truncate">${esc((actor&&actor.name)||'Пользователь')}</div><div class="text-xs text-slate-500 truncate">@${esc((actor&&actor.username)||'user')}</div></div></button>`; }
  function renderReports(){ const root=adminRoot(); if(!root) return; const active=window.adminReportsSubtab||'user'; const reports=(window.adminPanelData?.reports||[]).filter(r=>String(r.type||'user')===active); const statusBadge=s=>s==='pending'?badge('bell','Ожидает'):(s==='approved'?badge('check','Одобрено'):badge('x','Отклонено')); root.innerHTML=`<div class="ap3-tabs"><button type="button" class="ap3-tab ${active==='user'?'is-active':''}" id="ap3-rep-user">На пользователей</button><button type="button" class="ap3-tab ${active==='message'?'is-active':''}" id="ap3-rep-msg">На сообщения</button></div>${reports.length?`<div class="ap3-list">${reports.map(r=>`<div class="ap3-card"><div class="flex items-start justify-between gap-3"><div><div class="font-bold">${esc(r.reason_label||'Жалоба')}</div><div class="text-xs text-slate-500 mt-1">${esc(r.created_at_formatted||'')}</div></div><div>${statusBadge(r.status)}</div></div><div class="ap3-two mt-3">${actorCard(r.reporter_profile||{},'Кто подал')}${actorCard(r.target_profile||{},active==='message'?'На чьё сообщение':'На кого подана')}</div>${r.message&&r.message.message_id?`<div class="ap3-message"><div class="text-xs text-slate-400">Сообщение</div><div class="mt-2 whitespace-pre-wrap">${esc(r.message.preview||'')}</div></div>`:''}${r.comment?`<div class="ap3-quote mt-3">${esc(r.comment)}</div>`:''}${r.evidence&&r.evidence.url?`<div class="ap3-evidence">${r.evidence.kind==='photo'?`<img src="${esc(r.evidence.url)}" alt="">`:badge('note','Файл')}<div class="min-w-0"><div class="text-xs text-slate-400">Доказательства</div><a href="${esc(r.evidence.url)}" target="_blank" rel="noopener" class="text-sky-300 break-all">${esc(r.evidence.name||r.evidence.url)}</a></div></div>`:''}${r.admin_comment?`<div class="text-xs text-slate-400 mt-3">Комментарий модератора: ${esc(r.admin_comment)}</div>`:''}${r.status==='pending'?`<div class="ap3-actions"><button type="button" class="primary-button text-xs px-3 py-2" onclick="openReportReviewModal('${jsq(r.id)}','${jsq(r.target_user_id||'')}')">Рассмотреть</button><button type="button" class="secondary-button text-xs px-3 py-2" onclick="quickRejectReport('${jsq(r.id)}')">Отклонить</button></div>`:''}</div>`).join('')}</div>`:'<div class="ap3-empty">Жалоб в этой вкладке пока нет</div>'}`; document.getElementById('ap3-rep-user')?.addEventListener('click',()=>{window.adminReportsSubtab='user'; renderReports();}); document.getElementById('ap3-rep-msg')?.addEventListener('click',()=>{window.adminReportsSubtab='message'; renderReports();}); }
  function renderChannels(){ const root=adminRoot(); if(!root) return; const channels=window.adminPanelData?.channels||[]; root.innerHTML=`<div class="ap3-search">${window.adminSvg?window.adminSvg('search'):''}<input id="ap3-chan-search" type="text" placeholder="Поиск по названию канала или @username"></div><div id="ap3-chan-list" class="ap3-list"></div>`; const draw=q=>{ q=(q||'').toLowerCase().trim(); const items=!q?channels:channels.filter(ch=>String(ch.name||'').toLowerCase().includes(q)||String(ch.username||'').toLowerCase().includes(q)); const list=document.getElementById('ap3-chan-list'); if(list) list.innerHTML=items.length?items.map(ch=>`<div class="ap3-card"><div class="ap3-row"><div class="ap3-meta"><div class="ap3-name"><span>${esc(ch.name||'Канал')}</span>${ch.is_verified&&window.verifiedBadgeSvg?window.verifiedBadgeSvg('lg'):''}</div><div class="ap3-sub">@${esc(ch.username||'')}</div><div class="text-xs text-slate-500 mt-1">Участников: ${Number(ch.member_count||0)}</div></div><div class="ap3-side"><button type="button" class="${ch.is_verified?'secondary-button':'primary-button'} text-xs px-3 py-2" onclick="confirmChannelVerification('${jsq(ch.id)}','${jsq(ch.name||'Канал')}', ${ch.is_verified?'false':'true'})">${ch.is_verified?'Снять галочку':'Выдать галочку'}</button></div></div></div>`).join(''):'<div class="ap3-empty">Каналы не найдены</div>'; }; draw(''); document.getElementById('ap3-chan-search')?.addEventListener('input',e=>draw(e.target.value)); }
  function renderRoles(){ const root=adminRoot(); if(!root) return; const rows=window.adminPanelData?.role_policies||[]; root.innerHTML=`<div class="ap3-list">${rows.length?rows.map(r=>`<div class="ap3-card"><div class="font-bold text-lg">${esc(r.name||r.key)}</div><div class="text-xs text-slate-500 mb-2">${esc(r.key||'')}</div><div class="ap3-chips">${(r.rights||[]).map(x=>badge('shield',x)).join('')}</div></div>`).join(''):'<div class="ap3-empty">Политики ролей не найдены</div>'}</div>`; }
  window.__legacyRenderAdminPanelView = async function(){ const ok=await window.loadAdminData(); if(!ok){ if(window.showModal) showModal('Ошибка','Не удалось загрузить админ-панель'); return; } ensureScroll(true); const data=window.adminPanelData||{}; const active=data.activeTab||'users'; const html=`<div id="admin_panel_v3"><div class="ap3-header"><button type="button" class="secondary-button" onclick="switchScreen('chats_list')">${window.adminSvg?window.adminSvg('chat'):''}<span>К чатам</span></button><div class="ap3-title"><h2>Админ-панель</h2><p>Рабочая версия без закреплённых слоёв и скрытых контейнеров.</p></div></div><div class="ap3-stat-grid">${stat('users',data.stats?.total_users||0,'Всего пользователей')}${stat('bell',data.stats?.online_now||0,'Сейчас в мессенджере')}</div><div class="ap3-tabs">${TABS.map(t=>`<button type="button" class="ap3-tab ${t.key===active?'is-active':''}" data-tab="${t.key}">${window.adminSvg?window.adminSvg(t.icon):''}<span>${t.title}</span></button>`).join('')}</div><div id="admin-panel-v3-content"></div></div>`; const sc=document.getElementById('screen-container'); if(!sc) return; sc.innerHTML=`<div id="admin_screen_content" class="admin-panel-v3-root">${html}</div>`; window.adminPanelData.activeTab=active; document.querySelectorAll('#admin_panel_v3 .ap3-tab').forEach(btn=>btn.addEventListener('click',()=>window.switchAdminTab(btn.dataset.tab))); window.switchAdminTab(active); requestAnimationFrame(()=>{ try{ sc.scrollTop=0; }catch(e){} }); };
  window.switchAdminTab=function(tab){ if(!window.adminPanelData) return; window.adminPanelData.activeTab=tab; document.querySelectorAll('#admin_panel_v3 .ap3-tab').forEach(btn=>btn.classList.toggle('is-active', btn.dataset.tab===tab)); if(tab==='users') return renderUsers(); if(tab==='banned') return renderBanned(); if(tab==='reports') return renderReports(); if(tab==='channels') return renderChannels(); return renderRoles(); };
  window.selectAdminUser=function(userId){ const user=(window.adminPanelData?.users||[]).find(u=>String(u.id)===String(userId)); if(!user) return; const modal=document.getElementById('custom-modal'); if(!modal) return; const title=document.getElementById('modal-title'); const message=document.getElementById('modal-message'); const ok=document.getElementById('modal-ok-button'); const confirm=document.getElementById('modal-confirm-buttons'); const myKeys=myRoleKeys(); const myMax=maxRole(myKeys, !!window.currentUser?.is_moderator); const targetKeys=rolesNorm(user.roles).map(r=>String(r.key||'')); const canManage=canManageTarget(user); const canAssignSpecial=!!window.adminPanelData?.can_assign_special; const roleList=[['project_team','Команда проекта'],['special_admin','Спец Админ'],['owner1','Владелец 1'],['owner2','Владелец 2'],['main_admin','Главный Админ'],['deputy_admin','Зам. Главного Админа'],['senior_admin','Старший администратор'],['admin','Администратор'],['senior_moderator','Старший модератор'],['moderator','Модератор'],['junior_moderator','Младший модератор'],['helper','Хелпер']]; const roleRows=roleList.map(([key,label])=>{ const special=['project_team','special_admin','owner1','owner2'].includes(key); const rolePriority=roleMap()[key]||0; const hasRole=targetKeys.includes(key); const allowed=canManage && myMax>rolePriority && (!special||canAssignSpecial) && !myKeys.includes(key); const verb=hasRole?'removerole':'setrole'; return `<div class="admin-v2-role-row"><div><div class="font-semibold text-white">${esc(label)}</div><div class="text-xs text-slate-500">${esc(key)}</div>${!allowed?'<div class="text-xs text-red-300 mt-1">Недостаточно прав для изменения этой роли</div>':''}</div><button type="button" class="${hasRole?'secondary-button':'primary-button'} text-xs px-3 py-2" ${allowed?'': 'disabled aria-disabled="true"'} onclick="${allowed?`adminAction('${jsq(user.id)}','${verb}','${key}')`:''}">${hasRole?'Снять':'Выдать'}</button></div>`; }).join(''); title.textContent=`Управление: ${user.name||user.username||'Пользователь'}`; message.innerHTML=`<div class="space-y-4 text-left"><div class="admin-v2-modal-head"><img src="${esc(user.avatar||'/default.png')}" class="ap3-avatar" alt=""><div class="min-w-0"><div class="ap3-name"><span>${esc(user.name||user.username||'Пользователь')}</span>${user.is_verified&&window.verifiedBadgeSvg?window.verifiedBadgeSvg('lg'):''}</div><div class="ap3-sub">@${esc(user.username||'user')}</div><div class="ap3-pres ${user.online?'text-green-400':'text-slate-400'}">${esc(presence(user))}</div></div></div><div class="ap3-two"><div class="ap3-card"><div class="text-xs text-slate-400">Роли</div><div class="ap3-chips mt-2">${rolesNorm(user.roles).map(r=>`<span class="role-tag role-fx text-xs" style="background:${esc(r.color_bg||'#334155')};color:${esc(r.color_text||'#fff')}">${esc(r.name||r.key||'')}</span>`).join('') || '<span class="text-slate-500 text-sm">Нет ролей</span>'}</div></div><div class="ap3-card"><div class="text-xs text-slate-400">Модерация</div><div class="ap3-chips mt-2">${user.is_moderator?badge('shield','Модератор'):''}${(user.muted_until&&(user.muted_until*1000>Date.now()))?badge('bell','Мут до '+new Date(user.muted_until*1000).toLocaleString()):'<span class="text-slate-500 text-sm">Нет активного мута</span>'}</div></div></div><div class="ap3-actions"><button type="button" class="primary-button text-xs px-3 py-2" ${canManage?'':'disabled'} onclick="${canManage?`adminAction('${jsq(user.id)}','mute',{reason:'Нарушение правил',hours:24})`:''}">${window.adminSvg?window.adminSvg('bell'):''}<span>Мут 24ч</span></button><button type="button" class="secondary-button text-xs px-3 py-2" ${canManage?'':'disabled'} onclick="${canManage?`adminAction('${jsq(user.id)}','unmute')`:''}">${window.adminSvg?window.adminSvg('unlock'):''}<span>Снять мут</span></button><button type="button" class="primary-button text-xs px-3 py-2 bg-red-600 hover:bg-red-700" ${canManage?'':'disabled'} onclick="${canManage?`openAdminBanModal('${jsq(user.id)}')`:''}">${window.adminSvg?window.adminSvg('block'):''}<span>Бан</span></button><button type="button" class="secondary-button text-xs px-3 py-2" ${canManage?'':'disabled'} onclick="${canManage?`adminAction('${jsq(user.id)}','${user.is_verified?'unverify_user':'verify_user'}')`:''}">${window.adminSvg?window.adminSvg('verified'):''}<span>${user.is_verified?'Снять галочку':'Выдать галочку'}</span></button></div><div class="ap3-card"><div class="font-bold mb-2">Управление ролями</div>${roleRows}</div></div>`; ok.classList.remove('hidden'); ok.textContent='Закрыть'; ok.onclick=()=>modal.classList.add('hidden'); confirm.classList.add('hidden'); modal.classList.remove('hidden'); };
  const oldSwitchScreen = window.switchScreen;
  if (typeof oldSwitchScreen === 'function' && !window.__adminV3SwitchPatched){
    window.__adminV3SwitchPatched = true;
    window.switchScreen = function(screenName, ...args){ if(screenName !== 'admin_panel') ensureScroll(false); return oldSwitchScreen.call(this, screenName, ...args); };
  }
})();
</script>